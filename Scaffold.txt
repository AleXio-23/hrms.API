const [authorisedUser, setAuthorisedUser] = useState<IUser | null>();

  useEffect(() => {
    let storedUser = localStorage.getItem("authorisedUser");
    let user: IUser | null = null;
    if (storedUser) {
      user = JSON.parse(storedUser) as IUser;
      setAuthorisedUser(user);
    }
